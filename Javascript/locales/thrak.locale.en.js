/*==============================================================================================================

    Locale for en, en-US.

  ==============================================================================================================*/

  ( function ( $ )
   {
	var	this_locale = $. locale ( ). options ;

	// Set locale options
	$. locale ( 'en'   , this_locale ) ;
	$. locale ( 'en-US', this_locale ) ;
    } ( jQuery ) ) ;